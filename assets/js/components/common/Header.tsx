import * as React from "react";

const Header: React.FC = () => (
  <header>
    <section className="container" style={{ fontFamily: "Audiowide, cursive" }}>
      Progressions
    </section>
  </header>
);

export { Header };
